# CLAUDE.md — 開発ガイドライン

## バグ修正の進め方（必須）

バグ報告を受けて提案した修正が **1度でも効かなかった場合**、
次の修正案を自分で考えて提案してはならない。
必ずユーザーに追加調査を依頼し、新たな手がかりをもらってから判断すること。

> 理由：原因の誤認識のまま修正を重ねると、コードが汚染され問題の特定がさらに困難になる。

## 環境変数（必須）

新機能の実装で環境変数が必要になった場合、コードと同じタイミングで
`.env` にキー名だけの空エントリを追加すること（値は空欄でよい）。

```
NEW_API_KEY=
```

> 理由：実装後に「.envに追加して」と別途案内するのは漏れやすく、
> デプロイ時に気づかず本番が壊れる原因になる。

## デプロイ前チェック（必須）
コードを変更したらプッシュ前に必ず `npx tsc --noEmit` を実行し、
型エラーが 0 件であることを確認してからコミットすること。

## webpack 本番ビルドで `Super constructor null` が出たときの対処（禁止事項あり）

### 原因（確定）
webpack 公式 issue #17711 のバグ。rollup / TypeScript がカンマ演算子で複数クラスを
一つの変数宣言にまとめた出力に対し、webpack の **innerGraph（pure expression 解析）**
が継承元クラスを「未使用」と誤判定して `null` に差し替える。
`next dev`（開発ビルド）ではこの最適化が働かないため再現せず、
`next build`（本番ビルド）でのみ発生する。

### 正しい修正
`next.config.js` の webpack 設定で `optimization.innerGraph = false` を設定する：

```js
webpack: (config) => {
  config.optimization.innerGraph = false;
  return config;
}
```

### 禁止事項
次の対処は **原因と無関係なので絶対に行わないこと**：
- `transpilePackages` に Shotstack / pixi.js を追加する
- `resolve.alias` で pixi.js を別バンドルにリダイレクトする
- `NormalModuleReplacementPlugin` で pixi.js サブパスを置き換える
- `concatenateModules: false` でスコープホイスティングを無効化する

上記はすべて「pixi.js が原因」という誤った仮説に基づく対処であり、
実際の原因（webpack innerGraph バグ）には効果がない。

## 新規モデル追加時のチェックリスト（必須）

画像・動画・音声モデルを新規追加した際は、**実装前に**ヒスイAIの各機能との対応を確認し、
未対応機能をユーザーへ報告してから実装すること。

### 画像モデル 確認項目

| 機能 | 確認内容 |
|------|---------|
| テキストから生成 | `/generations` 系エンドポイントが存在するか |
| 参照して生成（imgUrl） | `/edits` 系エンドポイントが存在するか |
| スタイルテンプレート | 参照画像 + スタイル指示が機能するか |
| ファイルから参照 / 画像アップロード | 参照画像と同様 |
| 出力サイズ指定 | モデルが対応するサイズのみ送信しているか |
| 出力フォーマット（PNG/JPEG/WebP） | モデルがパラメータをサポートするか |

### 動画モデル 確認項目

| 機能 | 確認内容 |
|------|---------|
| テキストから動画 | テキストのみで生成可能か |
| 画像から動画（imgUrl） | first_frame / image 指定に対応しているか |
| 音声生成オプション | `generateAudio` パラメータが有効か |
| カメラ固定（cameraFixed） | モデルがパラメータをサポートするか |

### 実装ルール

① 新規モデル追加後、**APIが対応していない機能**を上記表で特定し、ユーザーへ報告すること

② UIでボタンが押せる状態なのにAPIが未対応の機能がある場合：
   - そのモデル選択時に該当ボタン・オプションを `disabled` にする、または非表示にする
   - 後回し実装の場合も必ずユーザーへ報告すること

> 理由：使えない機能がUIに存在すると、ユーザーがクレジットを消費したまま
> 意図しない結果になる。GPT Image 2 の参照画像未対応がその典型例。
